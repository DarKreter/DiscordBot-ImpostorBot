Impostor bot pretending to be someone from Radiestezja
-

**Install requirements:**
```
sudo apt update
sudo apt install $(cat requirements/apt.txt)
pip3 install -r requirements/pip.txt
```

**Run bot**: `python3 bot.py`
